// Logical Operators
// (|| - OR), (&& - AND), !

const name = 'peter';
const age = 30;

// if(name !== 'bob' || age < 21) {
//   console.log('hello there user');
// } else {
//   console.log('wrong values');
// }

if(name !== 'bob' && age < 21) {
  console.log('hello there user');
} else {
  console.log('wrong values');
} 